{
  "name": "npl-enhanced-content",
  "version": "0.1.0",
  "description": "NPL-enhanced content — Unicode-block semantic markup rendered by Lit web components. Zero React.",
  "type": "module",
  "//exports.": "'.' points at dist/npl.js, an IIFE global script with no ESM/CJS exports. 'type: module' advertises an ESM package; a bundler resolving '.' via import gets a script it cannot import from. Known conflict, tracked as ROADMAP.md debt D9 — do not import '.' from JS; use it only via <script> tag until M5 resolves the entry format.",
  "exports": {
    ".": "./dist/npl.js",
    "./themes/*": "./themes/*"
  },
  "files": ["dist", "themes"],
  "scripts": {
    "build": "vite build",
    "test": "cypress run --e2e",
    "test:open": "cypress open --e2e",
    "test:unit": "vitest run",
    "serve": "vite preview --port 4173"
  },
  "dependencies": {
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "cypress": "^15.21.1",
    "happy-dom": "^20.14.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^5.0.0"
  }
}
